     - add promise catches to all functions that use them?
     - Handle min and max when lowest/highest = ""

     - Warn
          - (datejust EX) modles with many sub modles and combinations can return bad data
          - Can't filter bracelet type
          - Can't filter indices type

     - Make all companies return promises?
          - use recursion to re-call them with same indicies?

     - make chrono change pages