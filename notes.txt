     - add promise catches to all functions that use them?

    - special watches are to be handled in "./utilityFunctions" in specialSites obj

     - make the average come from the current scrape (in data.csv) *************************

     - luxury bazaar 116610LV-0002 (lowLink)
     - Handle min and max when lowest/highest = ""

     - Warn about date justs
          - modles with many sub modles and combinations can return bad data